# Requested Visual Revision

## Updated

- Rebuilt **Discover by wellness focus** so the selected feature is one uninterrupted image card rather than an image stacked over a large white block.
- Rebuilt all wellness-focus selectors as full-bleed visual cards with compact readable overlays.
- Used the supplied `Images(2).zip` assets directly under `public/images/source/`; no new AI imagery was generated.
- Replaced **Shop by routine** card grid with an original six-stage **wellness rhythm** slider. It uses the cinematic reference only as an interaction cue and does not copy its ocean, time-of-day visuals, wording, canvas implementation, or layout.
- Rebuilt **Ingredient library** as a dimensional botanical book with a cover, page texture, central spine, deep shadow, botanical plates, chapter tabs, previous/next controls, and a 3D page-turn animation.
- Kept the existing hero carousel component and behavior unchanged. Only its written copy was refined.

## Validation

`node scripts/validate-project.mjs` passed: source files, local imports, and public image references validated.
